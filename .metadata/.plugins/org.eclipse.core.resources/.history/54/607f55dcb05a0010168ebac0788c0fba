<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<!DOCTYPE html>
<html>
<head>
    <title>BMI Calculator - FitSore</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background: url('images/bmi-bg.png') no-repeat center center fixed;
            background-size: cover;
            margin: 0;
            padding: 0;
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
            backdrop-filter: brightness(0.9);
        }

        .video-box, .form-box {
            background: white;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0,0,0,0.2);
            margin: 15px;
            padding: 20px;
            max-width: 400px;
            width: 100%;
        }

        iframe {
            width: 100%;
            height: 230px;
            border: none;
            border-radius: 10px;
        }

        .form-box h2 {
            margin-bottom: 20px;
            text-align: center;
            font-weight: 600;
        }

        input[type="number"] {
            width: 100%;
            padding: 14px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 16px;
        }

        button {
            width: 100%;
            padding: 14px;
            font-size: 16px;
            border: none;
            background-color: #007bff;
            color: white;
            border-radius: 8px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }

        .result {
            margin-top: 20px;
            font-size: 18px;
            text-align: center;
            font-weight: bold;
        }

        @media screen and (max-width: 768px) {
            .container {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>

<div class="container">

    <!-- Video Embed -->
    <div class="video-box">
        <iframe 
            src="https://www.youtube.com/embed/xF1W9wM0hso" 
            title="What BMI doesn't tell you about your health" 
            allowfullscreen>
        </iframe>
    </div>

    <!-- BMI Calculator Form -->
    <div class="form-box">
        <h2>BMI Calculator</h2>
        <input type="number" id="height" placeholder="Height (in cm)" required>
        <input type="number" id="weight" placeholder="Weight (in kg)" required>
        <button onclick="calculateBMI()">Calculate BMI</button>
        <div class="result" id="bmiResult"></div>
    </div>
</div>

<script>
    function calculateBMI() {
        const height = parseFloat(document.getElementById("height").value);
        const weight = parseFloat(document.getElementById("weight").value);
        const result = document.getElementById("bmiResult");

        if (!height || !weight || height <= 0 || weight <= 0) {
            result.innerText = "Please enter valid height and weight.";
            result.style.color = "red";
            return;
        }

        const bmi = weight / ((height / 100) ** 2);
        let category = "";

        if (bmi < 18.5) category = "Underweight";
        else if (bmi < 24.9) category = "Normal";
        else if (bmi < 29.9) category = "Overweight";
        else category = "Obese";

        result.innerText = `Your BMI is ${bmi.toFixed(2)} (${category})`;
        result.style.color = "#007bff";
    }
</script>

</body>
</html>
